#!/bin/sh
# Hermes pre-commit audit hooks intentionally disabled.
# Release-only source auditing is explicit; ordinary commits never scan the
# repository, data directories or temporary files.
exit 0
